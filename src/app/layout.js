export const metadata = {
  title: 'JASO | studio',
  description: 'jaso studio',
}

export default function RootLayout({ children }) {
 return (
    <html lang="es">
      <body>{children}</body>
    </html>
  )
}
